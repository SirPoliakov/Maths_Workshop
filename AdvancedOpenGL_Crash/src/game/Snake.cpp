#include "Snake.h"

void Snake::load(CubeMesh* myCube)
    {
        // body.emplace_back(0.0f, 0.0f, myCube);
        // body.emplace_back(CUBE_OFFSET, 0.0f, myCube);
        // body.emplace_back(2*CUBE_OFFSET, 0.0f, myCube);
    }

// Snake interactions :
void Snake::handleTurn(){}

void Snake::collision(){}

void Snake::eat(){}

